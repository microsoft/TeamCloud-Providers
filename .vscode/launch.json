{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Provider",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}
